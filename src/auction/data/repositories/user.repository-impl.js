const { Result } = require("../../../core");

class UserRepositoryImpl {
  constructor(source, mapper) {
    this.source = source;
    this.mapper = mapper;
  }

  // implement add, update own_auctions and auctions arrays
}

module.exports = UserRepositoryImpl;
